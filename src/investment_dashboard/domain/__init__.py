"""Pure financial-math layer. No I/O, no DB, no HTTP.

Implementations land in Phase 2. This package is intentionally empty for now;
its existence reserves the layering boundary and keeps mypy --strict scoped
to this module per spec §2.
"""
