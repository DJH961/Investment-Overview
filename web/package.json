{
  "name": "investment-overview-web",
  "version": "4.13.0",
  "private": true,
  "type": "module",
  "description": "v3.0 Live Web Companion — read-only GitHub Pages front-end that decrypts the published portfolio blob and renders live KPIs.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@fontsource-variable/inter": "^5.2.8",
    "decimal.js": "10.6.0"
  },
  "devDependencies": {
    "@types/node": "26.0.0",
    "playwright-core": "^1.61.0",
    "typescript": "6.0.3",
    "vite": "8.0.16",
    "vitest": "4.1.9"
  }
}
